// Write a program to determine if a number 
// is even or odd number using  the ternary operators
let num = 1454
let validate = (num % 2 == 0) ? "Number is Even" : "Number is Odd"
console.log(validate);